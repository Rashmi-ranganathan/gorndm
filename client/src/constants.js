let parameterList = ['coins', 'invites', 'createdAt'];
let parameterOrder = ['desc', 'desc', 'asc'];
let network = 'http://localhost:3001';

export default {
  parameterList,
  parameterOrder,
  network
}
